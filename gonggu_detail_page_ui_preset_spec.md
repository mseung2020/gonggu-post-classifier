# 공구 상세페이지("공구 자세히 보기") UI 프리셋 스펙 (v0.1 초안)

## 1. 목적과 방법론

공구 상세페이지 하나를 렌더링하려면 여러 영역(히어로 이미지, 가격, 기간, AI요약, 혜택표, 상세이미지, 크리에이터, 원본콘텐츠 등)이 필요하고, 각 영역은 DB에 실제로 들어있는 값의 조합에 따라 다르게 표시돼야 합니다. 이 문서는 각 영역을 **유한한 개수의 "프리셋"(UI 변형)**으로 나누고, 실제 데이터(2026-08-18 스냅샷, [[data-snapshot-2026-08-18]])에서 그 프리셋들이 얼마나 자주 나타나는지 세어서 커버리지를 검증했습니다.

임의의 상품 레코드 하나 = 영역별 프리셋의 조합 (예: 히어로#2 + 가격#3 + 기간#1 + AI요약#1 + 혜택표(배송#2·할인율○·구성○·사은품×·쿠폰×) + 상세이미지#1)으로 표현된다는 게 핵심 아이디어입니다.

**분석 대상 쿼리:** `SELECT * FROM dev_gongguking.gonggu_post_product a LEFT JOIN dev_gongguking.gonggu_post_product_detail b ON a.id = b.post_product_id`

**"페이지가 실제로 열리는" 모수:** 이 상세페이지는 구매 링크가 확정된(`link_status='done'`) 상품만 해당한다고 보고, 이 모수(**9,960건**)를 기준으로 그룹 2~6의 비중(%)을 계산했습니다. unresolved/hold/error(17,690건, 이 페이지가 열리지 않는 상태)는 모수에서 제외했습니다.

**이 스냅샷만으로는 확정 못하는 부분(중요):** 이 쿼리는 `gonggu_post_product` + `gonggu_post_product_detail` 두 테이블만 조인한 것이라 (1) 플랫폼(인스타그램 vs 유튜브) 구분, (2) 크리에이터 구독자수·평균할인율, (3) 원본콘텐츠 제목/날짜, (4) 상세이미지 실제 장수(1:N `_image` 테이블) 정보가 없습니다. 그룹 1 / +1 / +2는 화면과 스키마 지식으로 구조만 제안하고, 정확한 비중은 `gonggu_post`/`gonggu_video` 부모 테이블과 `_image` 테이블까지 조인한 추가 쿼리로 검증이 필요합니다(3장에 필요한 쿼리 제안 포함).

---

## 2. 그룹 2 — 브랜드 + 상품명 + 가격 표시

`brand_name_kr`, `original_price`, `sale_price`, `discount_rate`, `discount_amount`의 유/무 조합. (분모 9,960)

| # | 프리셋 | 조건 | 건수 | 비중 | 화면 표현 |
|---|---|---|---|---|---|
| P1 | 가격정보 없음 | 원가·판매가·할인율·절약액 전부 NULL | 4,457 | 44.7% | 가격 영역 숨김 또는 "가격 확인 필요" |
| P2 | 판매가만 | sale만 있음 | 2,082 | 20.9% | "12,000원" 단독 표기 (할인 배지 없음) |
| P3 | 판매가+할인율(±절약액) | sale+rate(+amt), orig 없음 | 265 | 2.7% | "62% 12,000원" (원가 취소선 없음) |
| P4 | 완전판(원가/판매가/할인율/절약액) | 4개 전부 | 2,930 | 29.4% | 스크린샷 예시 그대로 — 취소선 원가 + 할인율 + 판매가 + "OOO원 절약" 배지 |
| P5 | 기타 희귀 조합 | 위 4개 외 나머지 (원가만/원가+판매가만/절약액만 등 9가지 세부 패턴) | 226 | 2.3% | P2/P4의 폴백 규칙으로 흡수 권장(아래 참고) |

P5 세부 내역(참고, 226건 = 원가+판매가 42 / 원가만 24 / 원가+판매가+할인율 17 / 판매가+절약액 15 / 원가+판매가+절약액 13 / 원가+할인율 7 / 절약액만 2 / 원가+절약액 2 / 판매가+할인율+절약액인데 원가만 없음 1). 실제로는 "**할인율만 있고 가격 자체가 없는 케이스(104건, 1.0%)**"가 별도로 있는데 이건 위 P3에 이미 포함시켰지만 실제 표시할 금액이 없어 할인율 배지만 단독 노출되는 예외 상태라 QA 시 따로 확인 권장.

**폴백 규칙 제안:** 원가+판매가는 있는데 할인율이 없으면 `(원가-판매가)/원가`로 자동 계산해 P4처럼 렌더링. 절약액은 있는데 할인율이 없으면 역산해서 배지 표시. brand_name_kr 자체는 전체의 22.9%(6,346/27,712)에서만 존재 — 브랜드명 없을 때 "브랜드 >" 링크 영역을 숨기는 프리셋도 별도로 필요합니다.

---

## 3. 그룹 3 — 공구기간 + 남은기간

`gonggu_start_date` / `gonggu_end_date` 유무 × `gonggu_stage`(시작전/진행중/종료/판단불가). (분모 9,960)

| # | stage | 시작일 | 종료일 | 건수 | 비중 | 기간 표시 | 남은기간 표시 |
|---|---|---|---|---|---|---|---|
| D1 | 종료 | ○ | ○ | 4,080 | 41.0% | "6.10~6.18" | "종료됨" |
| D2 | 판단불가 | × | × | 2,562 | 25.7% | "기간 미상" | 배지 없음 또는 "정보없음" |
| D3 | 종료 | × | ○ | 1,484 | 14.9% | "~6.18" (시작일 미상) | "종료됨" |
| D4 | 종료 | ○ | × | 1,438 | 14.4% | "6.10~" (**강제종료** — [[gonggu-pipeline-overview]] FORCE_END_AFTER_DAYS 규칙, 종료일은 지어내지 않고 NULL 유지) | "종료됨(종료일 미상)" |
| D5 | 진행중 | ○ | × | 115 | 1.2% | "6.10~" | "진행중 · 마감일 미정" |
| D6 | (NULL) | × | × | 111 | 1.1% | 이례 상태 — D2와 동일 처리 권장 | - |
| D7 | 진행중 | ○ | ○ | 107 | 1.1% | "6.10~6.18" | **스크린샷 예시**: D-day 카운트다운("오늘 자정 마감"/"오늘 오후 6시 마감"/"N일 남음") |
| D8 | 진행중 | × | ○ | 42 | 0.4% | "~6.18" | D-day 카운트다운(시작일 미상) |
| D9 | 시작전 | ○ | × | 12 | 0.1% | "6.10~" | "시작까지 D-N" |
| D10 | 시작전 | ○ | ○ | 9 | 0.1% | "6.10~6.18" | "시작까지 D-N" |

**"남은기간" 텍스트는 별도 계산 규칙(오늘 날짜와 종료일의 차이)**이라 DB 값 조합이 아니라 로직으로 처리해야 합니다 — 제안 구간: D-day≥2 "N일 남음" / D-day=1 "내일 마감" / D-day=0 "오늘 OO시 마감"(원본 시간 있으면) 또는 "오늘 자정 마감"(없으면) / D-day<0인데 stage가 아직 종료로 안 바뀐 경우(rescan 지연) "마감 임박" 정도로 방어.

---

## 4. 그룹 4 — AI 공구요약

`ai_summary` 유무 × `ai_summary_confidence`는 **정확히 `detail_status`와 1:1로 묶여 있었습니다** (분모: done 링크 9,960 중 상세수집 성공 7,448건).

| # | 프리셋 | 조건 | 건수 | 비중(7,448 중) | 화면 표현 |
|---|---|---|---|---|---|
| A1 | 요약 없음 | detail_status가 done이 아님(blocked 1,878 / gone 633) 또는 애초에 detail row 없음(unresolved 등) | - | - | "AI 요약 준비 중" 또는 섹션 숨김 |
| A2 | 신뢰도 낮음 | done, confidence ≤50 | 799 | 10.7% | 신뢰도 배지 경고색(예: 회색/주황) |
| A3 | 신뢰도 중간 | done, 50<confidence≤70 | 1,552 | 20.8% | 배지 중립색 |
| A4 | 신뢰도 높음 | done, 70<confidence≤85 | 3,153 | 42.3% | 배지 기본색(스크린샷의 81%가 여기) |
| A5 | 신뢰도 매우높음 | done, confidence>85 | 1,944 | 26.1% | 배지 강조색 |

즉 이 영역은 "요약이 있다/없다"만 detail_status로 갈리고, 있을 때의 배지 색상만 4단계 신뢰도 구간으로 나누면 끝 — 별도의 "요약은 있는데 신뢰도 없음" 같은 예외는 실데이터에 없었습니다.

---

## 5. 그룹 5 — 가격·혜택 정보 표

이 표는 조합형이 아니라 **행(row) 5개가 각각 독립적으로 있음/없음**으로 결정되는 구조로 설계하는 게 맞습니다(전체 조합을 다 세면 104가지가 나와 프리셋으로 감당 불가). 분모: detail_status=done 7,448건.

| 행 | 프리셋 | 조건 | 건수 | 비중 |
|---|---|---|---|---|
| 배송 | 무료배송 | `free_shipping=1` | 1,698 | 22.8% |
| 배송 | 유료배송(금액표시) | `free_shipping=0` & `shipping_fee` 있음 | 3,343 | 44.9% |
| 배송 | 정보없음 | `free_shipping` NULL & `shipping_fee`·`shipping_note` 없음 | 2,015 | 27.0% |
| 배송 | 텍스트 폴백 | `free_shipping` NULL이지만 `shipping_note`만 있음 | 378 | 5.1% |
| 할인율 | 있음/없음 | `discount_rate` or `discount_amount` | 3,323 / 4,125 | 44.6% / 55.4% |
| 구성 | 있음/없음 | `composition_info` | 4,010 / 3,438 | 53.8% / 46.2% |
| 사은품 | 있음/없음 | `gift_info` | 2,568 / 4,880 | 34.5% / 65.5% |
| 쿠폰 | 있음/없음 | `coupon_info` | 955 / 6,493 | 12.8% / 87.2% |
| 전체 | **6행 전부 없음** | 위 항목 모두 미존재 | 777 | 10.4% |

**설계 제안:** 값이 없는 행은 렌더링하지 않고(행 자체 숨김), 6행이 다 없는 777건(10.4%)만 "제공된 혜택 정보가 아직 없어요" 같은 빈 상태 하나로 별도 처리.

---

## 6. 그룹 6 — 상세정보(상품 사진) 섹션 — 스크린샷의 "있을 때/없을 때" 핵심

`link_status`(페이지 진입 조건) × `detail_status` × `thumbnail_url` 존재 여부. 분모: done 링크 9,960 (+ 참고로 이 페이지가 아예 안 열리는 unresolved/hold/error 17,690건도 표기).

| # | 프리셋 | 조건 | 건수 | 비중 | 화면 표현 |
|---|---|---|---|---|---|
| T0 | (참고) 페이지 미노출 | link_status ≠ done | 17,690 | - | 애초에 이 페이지 자체가 안 열림 |
| T1 | 사진 있음(완전판) | detail_status=done & thumbnail 있음 | 6,057 | 60.8% | **스크린샷 2·4** — 실제 제품 사진 캐러셀 |
| T2 | 정보는 있는데 사진 없음 | detail_status=done & thumbnail 없음 | 1,391 | 14.0% | **스크린샷 1·3** — 가격/기간/AI요약/혜택표는 채워지는데 상세정보 영역만 빈 회색 박스 |
| T3 | 수집 차단/대기 | detail_status=blocked (봇확인·타임아웃 등, [[phase2-detail-pipeline]]) | 1,878 | 18.9% | 가격/AI요약/혜택표까지 전부 비어있음 — "정보를 가져오는 중이에요" 류의 재시도 대기 상태로 T2와는 다르게 취급 권장 |
| T4 | 상품 삭제 확인 | detail_status=gone | 633 | 6.4% | "이 상품은 더 이상 판매되지 않아요" — T3(일시적)과 다른 영구 상태 메시지 |

**주의:** thumbnail_url은 히어로/대표이미지 1장 기준 근사치입니다. 실제 "상세정보 펼쳐보기"의 회색 박스에 몇 장이 쌓이는지는 `gonggu_post_product_image`(1:N, image_type='detail') 테이블을 조인해야 정확히 나옵니다 — T1/T2를 "0장/1장/2장이상"으로 더 세분화하려면 아래 쿼리로 추가 확인 권장:

```sql
SELECT pp.id, COUNT(i.id) AS detail_image_cnt
FROM dev_gongguking.gonggu_post_product pp
JOIN dev_gongguking.gonggu_post_product_detail d ON d.post_product_id = pp.id
LEFT JOIN dev_gongguking.gonggu_post_product_image i
  ON i.post_product_id = pp.id AND i.image_type = 'detail'
WHERE d.detail_status = 'done'
GROUP BY pp.id;
```

---

## 7. 그룹 1 — 히어로/미디어 (데이터 갭 — 추가 쿼리 필요)

화면상 히어로는 "제품 실사진 캐러셀(도트 인디케이터)" vs "크리에이터 원본 컷 1장 + 플랫폼 배지(유튜브/인스타)"로 갈립니다. 구조 제안:

- H1: 상세이미지 있음(그룹6의 T1과 동일 모수, 6,057건) → 제품 사진 캐러셀, 장수만큼 도트
- H2: 상세이미지 없음(T2/T3/T4/T0) → 원본 게시물·영상의 대표 컷 1장 + 플랫폼 배지(유튜브▶ / 인스타그램)

이 스냅샷엔 게시물이 인스타(`gonggu_post`)인지 유튜브(`gonggu_video`)인지 구분하는 컬럼이 없어(쿼리가 post_product만 조회) H2 안에서 플랫폼별 비중은 못 냈습니다. `gonggu_post`/`gonggu_video` 각각에서 이 상품이 속한 post_id 개수를 세는 방식으로 후속 확인이 필요합니다.

## +1 — 크리에이터 카드

스크린샷엔 유튜브형(구독자수·평균 할인율)만 보입니다. 인스타그램 크리에이터는 팔로워수/다른 지표를 쓸 가능성이 커서, `gonggu_post`(인스타) 쪽 크리에이터 관련 컬럼을 확인해야 정확한 프리셋(유튜브형 vs 인스타형)을 나눌 수 있습니다. 현재 스냅샷엔 이 정보가 없습니다.

## +2 — 원본콘텐츠 + 비슷한 공구 리스트

원본콘텐츠 카드(썸네일+제목+플랫폼+날짜)와 비슷한 공구 리스트는 위 그룹 1(히어로)·그룹3(마감라벨)·그룹2(가격)의 프리셋을 축소 재사용하는 조합형 영역으로 보입니다. 마찬가지로 부모 테이블(post/video) 조인이 있어야 제목·날짜 등의 존재율을 확정할 수 있어 이번엔 구조만 제안합니다.

---

## 8. 다음 단계 제안

1. `gonggu_post`/`gonggu_video` + `gonggu_post_product_image`까지 포함한 조인 쿼리를 한 번 더 받으면 그룹 1 / +1 / +2까지 이 문서와 동일한 형식(조건·건수·비중)으로 채울 수 있습니다.
2. 그룹 2 P5(기타 226건), 그룹 3 D6(이례 111건) 같은 소수 예외는 디자인 시안엔 각각 하나의 "폴백 처리" 문구로 묶고, 실제 코드에서는 위에 적은 자동계산/방어 규칙으로 흡수하는 걸 권장합니다.
3. 이 문서 자체는 스냅샷 시점(2026-08-18) 기준 비중이라 시간이 지나면(특히 후반부 백필 진행에 따라 T3 blocked → T1/T4로 이동) 재검증이 필요합니다.
