공구집 일일 퀘스트 (2026-08-13 재정리 — enrich_detail 3단계 분리 반영)

# ════════════════════════════════════════════════════════════════════════
# 1. 공구 데일리 일일 퀘스트 (본줄기 — 수집~적재~백필)
# ════════════════════════════════════════════════════════════════════════

# ── 기본 실행 ──────────────────────────────────────
python3 -m gonggu.daily                                # 전체 무인 자동(핵심, 그냥 돌려두면 끝)
python3 -m gonggu.daily --list                          # 순서/기본값 확인

# ── 중단된 단계부터 이어서(--from <단계>) / 한 단계만(--only <단계>) ──
python3 -m gonggu.daily --from update_gonggu_stage      # 6.  공구 상태 갱신부터(=전체와 동일)
python3 -m gonggu.daily --from fetch_source             # 1.  원본 수집부터
python3 -m gonggu.daily --from fetch_yt_ppl             # 8-1. 유튜브 PPL 수집부터
python3 -m gonggu.daily --from classify                 # 2.  LLM 공구 분류부터
python3 -m gonggu.daily --from classify_yt_ppl          # 8-2. 유튜브 PPL 판별부터
python3 -m gonggu.daily --from transform                # 3.  게이트링부터
python3 -m gonggu.daily --from resolve_links            # 4.  링크 해석부터(Playwright — uc 아님)
python3 -m gonggu.daily --from load                     # 5.  DB 적재부터
python3 -m gonggu.daily --from rescan_inprogress        # 7.  진행중 재탐색부터
python3 -m gonggu.daily --from backfill_period_inpock   # 9-0. 기간 백필(인포크 우선, 크롤 없음)부터
python3 -m gonggu.daily --from backfill_period          # 9.  공구기간 백필(몰 크롤)부터
python3 -m gonggu.daily --from maintenance              # 10. 하우스키핑만 남았을 때
python3 -m gonggu.daily --only resolve_links            # 예: 링크 해석만 다시
python3 -m gonggu.daily --only load                     # 예: 적재만 다시

# ── 1.5. 이메일 DB 넣기 (daily 본줄기와 별도, 언제든 안전) ──
# 크리에이터 이메일을 hifen DB(instagram_user.email)에 반영. dev_gongguking엔 영향 없음.
python3 -m gonggu.sync_hifen_emails

# ── 1.5. 네이버 uc 갱신 (daily 끝난 뒤, 사람이 곁에서 · 낮은 동시성) ──
# 링크 해석(resolve_links) 단계에서 로그인월/차단으로 unresolved 남은 것만 uc로 재시도.
# 2단계 상세수집(섹션 2)의 uc와 크롬 하나를 공유하니 서로 동시 실행 금지, 워밍업은 1회로 둘 다 커버.
python3 -m gonggu.enrich_detail.warmup_naver_uc                                     # 워밍업 1회(창에서 로그인/확인 통과)
UC_LOGIN_WAIT=0 REVERIFY_CONCURRENCY=10 python3 -m gonggu.resolve_links.reverify_uc # unresolved(로그인월·차단)만 uc로 재시도 → done


# ════════════════════════════════════════════════════════════════════════
# 2. 공구 디테일 일일 퀘스트 (상세수집 — 확정된 링크를 열어 가격/이미지/설명 보강)
# ════════════════════════════════════════════════════════════════════════
# daily 본줄기가 끝난 뒤(링크가 done인 상품이 있어야 대상이 생김), 순서 무관하게 아래 중
# 필요한 것만 돌리면 됨. 2단 백필(운영 성격이 정반대인 두 패스, DB 상태 'blocked'로 결합):
#   fast — 무인·병렬·안정. 자사몰 대량 처리. 안티봇 걸리면 시도 없이/시도 후 blocked로 남김.
#   uc   — 사람이 곁에서. blocked인 것만(호스트 무관하게 대상이지만 실제 uc 필요한 건 네이버뿐
#          — 2026-08-13, 그 외 호스트는 자동으로 http/Playwright 먼저 시도 후 안 되면 uc 폴백).

# (1) fast 자사몰 대량 처리 — 동시성 기본 4, 올려도 ≤10(브라우저 상한 10, 100 절대 금지)
DETAIL_CONCURRENCY=10 python3 -m gonggu.enrich_detail

# (2) uc 백로그 — 예전 방식(상품 1건마다 크롤링+LLM+DB를 한 번에). 소량/단일 프로필이면 충분.
python3 -m gonggu.enrich_detail.warmup_naver_uc                                                # 워밍업(1.5와 공유, 이미 했으면 생략)
UC_LOGIN_WAIT=0 DETAIL_MODE=uc DETAIL_CONCURRENCY=1 python3 -m gonggu.enrich_detail             # reverify_uc/1.5와 동시 실행 금지

# (3) uc 백로그 — 3단계 분리(2026-08-13 대공사, 백로그가 커서 위 방식이 너무 느릴 때).
#   크롤링(브라우저, 느림) → LLM(고동시성 배치, 빠름) → DB적재를 완전히 독립된 명령으로 분리.
#   크롤링 결과(detail_crawled*.jsonl)는 LLM 결과와 별도 파일로 영구 보존 — 나중에 프롬프트
#   고쳐서 LLM만 다시 돌려도(FORCE_RELLM=1) 재크롤링 불필요.
#
#   3-1) 크롤링만 — 신뢰 쿠키 프로필을 N벌 복제해서(cp -R ~/.gonggu_uc_profile ~/.gonggu_uc_profile_N)
#        진짜 병렬(각자 다른 UC_PROFILE). 5개 예시:
UC_LOGIN_WAIT=0 DETAIL_MODE=uc UC_PROFILE=$HOME/.gonggu_uc_profile_1 SHARD_COUNT=5 SHARD_INDEX=0 python3 -m gonggu.enrich_detail.crawl_stage
UC_LOGIN_WAIT=0 DETAIL_MODE=uc UC_PROFILE=$HOME/.gonggu_uc_profile_2 SHARD_COUNT=5 SHARD_INDEX=1 python3 -m gonggu.enrich_detail.crawl_stage
UC_LOGIN_WAIT=0 DETAIL_MODE=uc UC_PROFILE=$HOME/.gonggu_uc_profile_3 SHARD_COUNT=5 SHARD_INDEX=2 python3 -m gonggu.enrich_detail.crawl_stage
UC_LOGIN_WAIT=0 DETAIL_MODE=uc UC_PROFILE=$HOME/.gonggu_uc_profile_4 SHARD_COUNT=5 SHARD_INDEX=3 python3 -m gonggu.enrich_detail.crawl_stage
UC_LOGIN_WAIT=0 DETAIL_MODE=uc UC_PROFILE=$HOME/.gonggu_uc_profile_5 SHARD_COUNT=5 SHARD_INDEX=4 python3 -m gonggu.enrich_detail.crawl_stage
#        (샤딩 없이 단일 프로세스로도 가능: SHARD_COUNT/SHARD_INDEX/UC_PROFILE 다 빼고 그냥)
UC_LOGIN_WAIT=0 DETAIL_MODE=uc python3 -m gonggu.enrich_detail.crawl_stage
#
#   3-2) LLM만 — 브라우저와 무관, 고동시성(기본 60)으로 크롤링 결과를 몰아서 처리
LLM_STAGE_CONCURRENCY=60 python3 -m gonggu.enrich_detail.llm_stage
FORCE_RELLM=1 python3 -m gonggu.enrich_detail.llm_stage        # 프롬프트 수정 후 이미 처리된 것까지 전부 재처리
#
#   3-3) DB 반영만
python3 -m gonggu.enrich_detail.load_stage


# ════════════════════════════════════════════════════════════════════════
# 3. 나머지 기타
# ════════════════════════════════════════════════════════════════════════

# ── 작업 반영(커밋+푸시, 언제든 안전) ──
#   .gitignore가 data/·auth 세션쿠키·.env를 전부 빼주므로 add -A라도 소스만 올라간다.
#   pull --rebase가 원격에 다른 커밋이 있어도 얹어준다(같은 줄 진짜 충돌이면 거기서 멈춰 수동 해결).
git add -A && git commit -m "작업 반영 $(date +%F)" && git pull --rebase origin main && git push

# ── 쿠팡/알리/테무 삭제(마켓플레이스 링크 소급 제거) ──
#   --yes 없으면 미리보기만, --status로 대상 상태 지정 가능
python3 -m gonggu.purge_marketplace_links --yes

# ── 대시보드 ──
python3 -m gonggu.unresolved_board --open

# ── 메모리 확인/정리 ──
ggstat / ggclean

# ── 일회성 스키마 마이그레이션(이미 반영했으면 다시 돌릴 필요 없음) ──
python3 -m gonggu._migrate_detail_blocked   # detail_status ENUM에 'blocked' 추가(상세 2단 백필 도입 시 1회)
